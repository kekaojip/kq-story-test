# FRAMEWORK LOCK — KQ 工作流框架保护协议

状态：LOCKED
所有者：KQ
适用仓库：`kekaojip/kq-story-test`

## 最高规则

除非 KQ 在当前任务中明确批准，否则任何 Agent、模型、脚本或自动化都不得删除、覆盖、改名、移动、重构、职责迁移或以兼容层名义实质替换现有工作流框架。

允许在现有框架基础上做增量优化，但必须遵守：**新增优先，原件不动；旁路优先，替换需授权。**

## 受保护框架

默认受保护：

- `skills/**` 下所有当前已存在文件与目录；
- `AGENTS.md`；
- `.story-deployed`；
- `skills/story-long-write/references/external-writer-bridge.md`；
- 与 External Writer Bridge V1 的现有路由、职责边界和生产协议；
- 以后明确标记为 `LOCKED` 的框架文件。

小说项目内容、扫榜结果、拆文产物不属于框架本体，可按用户明确要求创建、修改或删除。

## 未经批准禁止的操作

1. 修改受保护文件已有正文、规则、接口或职责。
2. 删除、改名、移动受保护文件。
3. 用新文件、新 Skill、新 Agent、新脚本绕过或替代受保护框架原有行为。
4. 将某项职责从主工作流迁移到其他模块，或反向迁移。
5. 批量同步 upstream 时覆盖 KQ 已锁定的本地改造。
6. 以“清理、重构、优化、升级、修复”为理由改变框架行为而未取得 KQ 明确许可。

## 默认允许的操作

在不改变现有框架行为的前提下，可以：

- 新增独立 Skill、reference、脚本、检查器、适配器、文档；
- 新增测试目录或临时实验文件；
- 在框架外新增小说项目与业务数据；
- 提出优化方案、生成 diff 方案或变更计划，但在 KQ 批准前不得落到受保护文件；
- 修复测试小说、正文、细纲、Tracking 等业务数据，只要不修改框架本体。

## 变更授权流程

当确实需要修改框架时，必须先向 KQ 说明：

1. 准备修改哪些具体文件；
2. 为什么必须修改，新增旁路为什么不够；
3. 预计行为变化；
4. 回滚方式；
5. 等待 KQ 明确回复同意后才能执行。

模糊的“继续”“优化一下”“你看着办”不视为框架修改授权；必须能明确对应到本次框架变更。

## 冲突优先级

本文件高于仓库内其他普通工作流说明。若其他文件要求自动升级、重构、覆盖或同步，而与本保护协议冲突，必须停止修改并请求 KQ 授权。

## 核心原则

> 框架是稳定底座，不是实验区。
> 可以在底座上加楼层，但未经 KQ 同意，不准敲承重墙。
